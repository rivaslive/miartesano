import { styled } from 'styles/stitches.config';

export const SpaceStyle = styled('div', {
  display: 'flex',
  alignItems: 'center',
  height: '100%',
  width: '100%',
  flexWrap: 'wrap',
});

export const SpaceItemStyle = styled('div', {
  padding: '0 20px',
});
