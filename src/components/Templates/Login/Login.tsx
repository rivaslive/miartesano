import CardLogin from 'components/Organism/CardLogin';
import { StyleContent, StyleWrapper } from './style';

const LoginTemplate = () => {
  return (
    <StyleWrapper>
      <StyleContent>
        <CardLogin />
      </StyleContent>
    </StyleWrapper>
  );
};

export default LoginTemplate;
